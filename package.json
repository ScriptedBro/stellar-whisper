{
  "name": "stellar-whisper-workspace",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend": "npm run dev --prefix frontend",
    "indexer": "npm run start --prefix indexer",
    "dev": "npx concurrently \"npm run indexer\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
